---
name: php-test
description: PHP のテスト作成時に守るべきルール
---

# PHP Test

## ルール

- テストコードは `tests/` ディレクトリに配置する。
- テストクラスは `*Test.php` という命名規則に従う。
- テストメソッドは `test*` という命名規則に従う。
- テストコードには必ずコメントを入れる。
- コメントは必ず日本語で説明を書く。
