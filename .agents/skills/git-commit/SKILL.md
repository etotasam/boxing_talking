---
name: git-commit
description: git commitの実行時専用
---

目的:

- git に変更を保存する

確認項目:

- 形式: `<type>(<scope>): <description>`
- `scope` は変更対象を簡潔に示してください。不要な場合は省略。
- `description`は日本語を使用する事。
- コミット前に、今回の変更だけがステージされていることを確認する。
